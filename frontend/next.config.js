/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  // Enable experimental features if needed
  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;

